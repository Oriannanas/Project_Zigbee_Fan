# Name,   Type, SubType, Offset,  Size, Flags
# Note: if you have increased the bootloader size, make sure to update the offsets to avoid overlap
# Two-OTA-slot layout sized for a 4MB flash chip (ESP32-C6FH4, confirmed via esptool flash_id).
# ota_0/ota_1 must stay 64K-aligned so either can be mapped as the boot app.
# esp-zigbee-lib 2.x stores Zigbee network/binding state in the "nvs" partition
# itself (not a separate zboss-style raw partition), hence the larger nvs size.
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xf000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x1E0000,
ota_1,      app,  ota_1,    0x200000, 0x1E0000,
zb_fct,     data, fat,      0x3E0000, 0x1000,
